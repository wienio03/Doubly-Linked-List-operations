#include "main.h"
/*void CreateList(NODE* firstNode, int argCount, ...){
    va_list arguments;
    va_start(arguments, argCount);
    for(int i=0; i<argCount; i++){
        int newObject = va_arg(arguments, int);
        if(i==0){
            firstNode->info = newObject;
            printf("%d object added\n", i);
        }
        else {
            Insert(&firstNode, &newObject);
            printf("%d object added\n", i);

        }
    }
    va_end(arguments);
}*/